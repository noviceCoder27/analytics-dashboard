{
    "version":2,
    "builds": [
      { "src": "*.js", "use": "@vercel/node" }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "/" }],
    "routes": [
        {
          "src": "/(.*)",
          "dest": "src/index.js",
          "methods":["GET"],
          "headers": {
            "Access-Control-Allow-Origin": "*"
          }
        }
    ],
    
}